diff --git a/dist/converters/completions.cjs b/dist/converters/completions.cjs
index 41c6a6b31101d3f2553d9844d55f6160712571fd..696ea04c690c83597563b692543ee20857bcfaaf 100644
--- a/dist/converters/completions.cjs
+++ b/dist/converters/completions.cjs
@@ -469,10 +469,15 @@ const convertStandardContentMessageToCompletionsMessage = ({ message, model }) =
 		role: "system",
 		content: message.contentBlocks.filter((block) => block.type === "text")
 	};
-	else if (role === "assistant") return {
-		role: "assistant",
-		content: message.contentBlocks.filter((block) => block.type === "text")
-	};
+	else if (role === "assistant") {
+		const assistantParam = {
+			role: "assistant",
+			content: message.contentBlocks.filter((block) => block.type === "text")
+		};
+		const reasoningContent = message.additional_kwargs?.reasoning_content;
+		if (reasoningContent != null) assistantParam.reasoning_content = reasoningContent;
+		return assistantParam;
+	}
 	else if (role === "tool" && __langchain_core_messages.ToolMessage.isInstance(message)) return {
 		role: "tool",
 		tool_call_id: message.tool_call_id,
@@ -589,6 +594,7 @@ const convertMessagesToCompletionsMessageParams = ({ messages, model }) => {
 			if (message.additional_kwargs.tool_calls != null) completionParam.tool_calls = message.additional_kwargs.tool_calls;
 			if (__langchain_core_messages.ToolMessage.isInstance(message) && message.tool_call_id != null) completionParam.tool_call_id = message.tool_call_id;
 		}
+		if (role === "assistant" && message.additional_kwargs.reasoning_content != null) completionParam.reasoning_content = message.additional_kwargs.reasoning_content;
 		if (message.additional_kwargs.audio && typeof message.additional_kwargs.audio === "object" && "id" in message.additional_kwargs.audio) {
 			const audioMessage = {
 				role: "assistant",
diff --git a/dist/converters/completions.js b/dist/converters/completions.js
index 7a7adf58a6f21e12494ece593c00255604cc60b2..21b23b4718491d083b201fc34ea7eb3086d16686 100644
--- a/dist/converters/completions.js
+++ b/dist/converters/completions.js
@@ -468,10 +468,15 @@ const convertStandardContentMessageToCompletionsMessage = ({ message, model }) =
 		role: "system",
 		content: message.contentBlocks.filter((block) => block.type === "text")
 	};
-	else if (role === "assistant") return {
-		role: "assistant",
-		content: message.contentBlocks.filter((block) => block.type === "text")
-	};
+	else if (role === "assistant") {
+		const assistantParam = {
+			role: "assistant",
+			content: message.contentBlocks.filter((block) => block.type === "text")
+		};
+		const reasoningContent = message.additional_kwargs?.reasoning_content;
+		if (reasoningContent != null) assistantParam.reasoning_content = reasoningContent;
+		return assistantParam;
+	}
 	else if (role === "tool" && ToolMessage.isInstance(message)) return {
 		role: "tool",
 		tool_call_id: message.tool_call_id,
@@ -588,6 +593,7 @@ const convertMessagesToCompletionsMessageParams = ({ messages, model }) => {
 			if (message.additional_kwargs.tool_calls != null) completionParam.tool_calls = message.additional_kwargs.tool_calls;
 			if (ToolMessage.isInstance(message) && message.tool_call_id != null) completionParam.tool_call_id = message.tool_call_id;
 		}
+		if (role === "assistant" && message.additional_kwargs.reasoning_content != null) completionParam.reasoning_content = message.additional_kwargs.reasoning_content;
 		if (message.additional_kwargs.audio && typeof message.additional_kwargs.audio === "object" && "id" in message.additional_kwargs.audio) {
 			const audioMessage = {
 				role: "assistant",
diff --git a/dist/converters/responses.cjs b/dist/converters/responses.cjs
index 33efbf5652bac3048bf138027b52da7c4896df18..54853972b3ab6ea5c100d416d57babf8c92a7623 100644
--- a/dist/converters/responses.cjs
+++ b/dist/converters/responses.cjs
@@ -909,13 +909,12 @@ const convertMessagesToResponsesInput = ({ messages, zdrEnabled, model }) => {
 					refusal: additional_kwargs.refusal
 				}];
 			}
-			if (typeof content === "string" || content.length > 0) input.push({
-				type: "message",
-				role: "assistant",
-				...lcMsg.id && !zdrEnabled && lcMsg.id.startsWith("msg_") ? { id: lcMsg.id } : {},
-				content: require_misc.iife(() => {
-					if (typeof content === "string") return content;
-					return content.flatMap((item) => {
+			const formattedContent = require_misc.iife(() => {
+				if (typeof content === "string") {
+					return content.length > 0 ? content : undefined;
+				}
+				if (Array.isArray(content) && content.length > 0) {
+					const flat = content.flatMap((item) => {
 						if (item.type === "text") return {
 							type: "output_text",
 							text: item.text,
@@ -924,7 +923,15 @@ const convertMessagesToResponsesInput = ({ messages, zdrEnabled, model }) => {
 						if (item.type === "output_text" || item.type === "refusal") return item;
 						return [];
 					});
-				})
+					return flat.length > 0 ? flat : undefined;
+				}
+				return undefined;
+			});
+			if (formattedContent != null) input.push({
+				type: "message",
+				role: "assistant",
+				...lcMsg.id && !zdrEnabled && lcMsg.id.startsWith("msg_") ? { id: lcMsg.id } : {},
+				content: formattedContent
 			});
 			const functionCallIds = additional_kwargs?.[_FUNCTION_CALL_IDS_MAP_KEY];
 			if (__langchain_core_messages.AIMessage.isInstance(lcMsg) && !!lcMsg.tool_calls?.length) input.push(...lcMsg.tool_calls.map((toolCall) => {
diff --git a/dist/converters/responses.js b/dist/converters/responses.js
index 8dc68d3e3b4fa9518604cd006bda3aff7762b228..81491ff056d1a97f72e0552fd71c6f2e02d3bcaf 100644
--- a/dist/converters/responses.js
+++ b/dist/converters/responses.js
@@ -908,13 +908,12 @@ const convertMessagesToResponsesInput = ({ messages, zdrEnabled, model }) => {
 					refusal: additional_kwargs.refusal
 				}];
 			}
-			if (typeof content === "string" || content.length > 0) input.push({
-				type: "message",
-				role: "assistant",
-				...lcMsg.id && !zdrEnabled && lcMsg.id.startsWith("msg_") ? { id: lcMsg.id } : {},
-				content: iife(() => {
-					if (typeof content === "string") return content;
-					return content.flatMap((item) => {
+			const formattedContent = iife(() => {
+				if (typeof content === "string") {
+					return content.length > 0 ? content : undefined;
+				}
+				if (Array.isArray(content) && content.length > 0) {
+					const flat = content.flatMap((item) => {
 						if (item.type === "text") return {
 							type: "output_text",
 							text: item.text,
@@ -923,7 +922,15 @@ const convertMessagesToResponsesInput = ({ messages, zdrEnabled, model }) => {
 						if (item.type === "output_text" || item.type === "refusal") return item;
 						return [];
 					});
-				})
+					return flat.length > 0 ? flat : undefined;
+				}
+				return undefined;
+			});
+			if (formattedContent != null) input.push({
+				type: "message",
+				role: "assistant",
+				...lcMsg.id && !zdrEnabled && lcMsg.id.startsWith("msg_") ? { id: lcMsg.id } : {},
+				content: formattedContent
 			});
 			const functionCallIds = additional_kwargs?.[_FUNCTION_CALL_IDS_MAP_KEY];
 			if (AIMessage.isInstance(lcMsg) && !!lcMsg.tool_calls?.length) input.push(...lcMsg.tool_calls.map((toolCall) => {
